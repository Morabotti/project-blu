export enum LocalStorageKey {
  Token = 'token'
}

export enum QueryParams {
  Redirect = 'redirect',
  Params = 'params',
  Limit = 'limit',
  Offset = 'offset',
  Search = 'search'
}

export enum UserRole {
  User = 0,
  Admin = 1
}

export enum AuthProvider {
  Microsoft = 1,
  Google = 2
}
